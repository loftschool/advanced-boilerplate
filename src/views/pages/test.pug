<!DOCTYPE html>
html(lang="en")
  head
    meta(charset="UTF-8")
    meta(name="viewport", content="width=device-width, initial-scale=1.0")
    meta(http-equiv="X-UA-Compatible", content="ie=edge")
    title Document
    style.
      .fade-enter-active, .fade-leave-active {
        transition: 2s;
        position: absolute;
        top: 0;
        left: 0;
      }

      .fade-enter {
        transform: translate(200px);
      }

      .fade-enter-to {
        transform: translate(0px);
      }

       .fade-leave {
        transform: translate(0);
      }

      .fade-leave-to {
        transform: translate(200px);
      }

      .transition-wrap {
        position: relative;
        
      }

      .block-wrap {
        position: fixed;
        top: 50%;
        left: 50%;
        transform: translate(-50%, -50%);
        width: 400px;
        height: 400px;
        perspective: 1000px;
      }


      .block-a {
        background: red;
        width: 400px;
        height: 400px;
      }

      .block-b {
        background: blue;
        width: 400px;
        height: 400px;
      }

      .transition-component {
        position: absolute;
        width: 100%;
        height: 100%;
      }

      .flip-enter-active, .flip-leave-active {
        transition: 1s linear;
      }

      .flip-enter {
         transform: rotateY(90deg);
      }

      .flip-enter-to {
         transform: rotateY(180deg);
      }

       .flip-leave {
           transform: rotateY(0deg);
      }

      .flip-leave-to {
          transform: rotateY(90deg);
      }
  body
    #animation-root



    script(type="text/x-template")#animation
      div
        pre {{currentComponent}}
        button(type="text" @click="currentComponent === 'compA' ? currentComponent = 'compB' : currentComponent = 'compA'") Показать / скрыть
        .transition-wrap
          .transition-component
            transition(name="fade")
              component(:is="currentComponent")
    
    script(type="text/x-template")#animation-block
      div
        button(type="text" @click="currentComponent === 'compA' ? currentComponent = 'compB' : currentComponent = 'compA'") Показать / скрыть
        .block-wrap
          transition(name="flip" mode="")
            component(:is="currentComponent")


    script(type="text/x-template")#component-a
      .block-a

    script(type="text/x-template")#component-b
      .block-b

    script(src="blog.bundle.js")